"use strict";
let userNames = ['admin', 'mahnoor', 'amna', 'yashfa', 'laiba'];
if (userNames.length === 0) {
    console.log('we need to find some users');
}
else {
    userNames = [];
    console.log('All users name has been removed. ' + userNames.length);
}
